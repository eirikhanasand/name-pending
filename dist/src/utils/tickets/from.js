export default function From() {
    // Lager en ticket ut fra meldinger i en eksisterende kanal
    // Vis en liste over meldinger i kanalen, og la brukeren velge hvilke meldinger som skal være med i ticketen
    // Kan enten klikke på hver melding for å legge til, eller skrive en kommando for å adde alle etter en viss melding, eller adde de siste x meldingene
}
